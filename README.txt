XML parser is implemented in python3 without use of existig libraries

xml files are also included

keep all the files in same folder and run the .py file

python3 xml_parser.py


XML is read as string. Tags are stored in an n-ary tree. tree  is traversed from root using given query path.

Assumptions:

1. XML file has no errors

2. No sibling tags has same name


